%h1
  =@team.name
- unless @characters.empty?
  = render partial: '/characters/character_table', locals: { characters: @characters }
- else
  = render partial: 'form'
= link_to 'Home', teams_path
= link_to 'Edit', edit_team_path(@team)
