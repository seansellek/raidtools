%table
  %tr
    %th Name
    %th Realm
    %th ilvl
    - @characters.first.items.each_key do |slot|
      %th
        =slot.to_s.capitalize
  - @characters.each do |c|
    = render :partial => 'character', :locals => { :c => c }
